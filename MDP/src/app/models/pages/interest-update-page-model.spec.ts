import { InterestUpdatePageModel } from './interest-update-page-model';

describe('InterestUpdatePageModel', () => {
  it('should create an instance', () => {
    expect(new InterestUpdatePageModel()).toBeTruthy();
  });
});
