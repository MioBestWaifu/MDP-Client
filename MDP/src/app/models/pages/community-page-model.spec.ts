import { CommunityPageModel } from './community-page-model';

describe('CommunityPageModel', () => {
  it('should create an instance', () => {
    expect(new CommunityPageModel()).toBeTruthy();
  });
});
