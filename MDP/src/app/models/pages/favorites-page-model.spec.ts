import { FavoritesPageModel } from './favorites-page-model';

describe('FavoritesPageModel', () => {
  it('should create an instance', () => {
    expect(new FavoritesPageModel()).toBeTruthy();
  });
});
